export default function Logo({ className = 'w-12 h-12' }) {
  return (
    <img 
      src="/Untitled design (49) (2).png"
      alt="Startup Kafé"
      className={`${className} object-contain`}
    />
  );
}
